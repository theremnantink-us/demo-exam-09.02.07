<?php
/* ============================================================================
   config.php — НАСТРОЙКИ ЛОГИКИ системы (данные, статусы, доступы).
   Внешний вид и наполнение главной — в отдельном файле theme.php.
   ----------------------------------------------------------------------------
   🔧 ПОД ТЕМУ ЗАДАНИЯ меняешь обычно только:
     1. $APP['entity_label*'] — как зовётся заявка (заявка/заказ/запись).
     2. $APP['courses']       — список курсов/услуг/товаров (≥10 строк для базы).
     3. $APP['statuses']      — статусы заявки из задания.
     4. $APP['payment_types'] — способы оплаты из задания.
     5. $APP['admin']         — логин/пароль администратора из задания.
   ========================================================================== */

require_once __DIR__ . '/theme.php';

// --- Подключение к БД (XAMPP по умолчанию: root без пароля) -----------------
const DB_HOST = '127.0.0.1';
const DB_PORT = '3306';
const DB_USER = 'root';
const DB_PASS = '';            // XAMPP: пусто. Если у MySQL есть пароль — впиши.
const DB_NAME = 'exam_db';

$APP = [
    // Как называется главная сущность (для заголовков и кнопок).
    'entity_label'        => 'заявка',
    'entity_label_gen'    => 'заявки',
    'entity_label_plural' => 'Заявки',

    // 🔧 Список курсов. Первые 3 — из задания-2026 (выпадающий список).
    //    Должно быть ≥10 для наполнения базы. На форме показываются все.
    'courses' => [
        'Основы алгоритмизации и программирования',
        'Основы веб-дизайна',
        'Основы проектирования баз данных',
        'Веб-разработка на PHP',
        'Основы Python',
        'Компьютерные сети',
        'Информационная безопасность',
        'Графический дизайн',
        'Системное администрирование',
        'Управление проектами',
    ],

    // 🔧 Способы оплаты (задание-2026).
    'payment_types' => ['Наличными', 'Перевод по номеру телефона'],

    // 🔧 Статусы заявки (задание-2026). Ключи в коде не трогай, меняй подписи.
    'statuses' => [
        'new'      => 'Новая',
        'studying' => 'Идёт обучение',
        'done'     => 'Обучение завершено',
    ],
    // Статус, при котором пользователю можно оставить отзыв (после обучения).
    'review_allowed_status' => 'done',

    // 🔧 Доступ в админку (задание-2026: Admin / KorokNET).
    'admin' => [
        'login'    => 'Admin',
        'password' => 'KorokNET',
    ],

    // Пагинация в админке (Модуль 2).
    'per_page' => 8,
];

function app($key = null) {
    global $APP;
    return $key === null ? $APP : ($APP[$key] ?? null);
}
