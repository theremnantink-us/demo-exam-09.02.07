# 🟢 Шпаргалка по проекту (демо-экзамен 09.02.07, издание 2026)

Универсальный шаблон ИС (главная → регистрация → заявки → отзывы → админка) с
**подсистемой безопасности**. Тема на экзамене другая — структура одинаковая.

---

## 1. Запуск (XAMPP, Windows)
1. Скопируй `exam` в `C:\xampp\htdocs\`.
2. Запусти **Apache** и **MySQL** в XAMPP.
3. Открой `http://localhost/exam/` — БД и тестовые данные создадутся сами.
4. Дали готовый файл БД? phpMyAdmin → Импорт → `database.sql`.

**Доступы:** пользователь `ivanov1` / `Parol12345`; админ `Admin` / `KorokNET`.

---

## 2. Переделать под свою тему

### Файл `theme.php` — ВНЕШНОСТЬ и НАПОЛНЕНИЕ (за дизайн дают баллы)
- `site_name`, `tagline`, `logo_text`/`logo_img` — название и логотип.
- `colors` — фирменные цвета (1–2 акцента под тему).
- `fonts` — шрифты. ⚠️ На экзамене НЕТ интернета: либо вставь Google-ссылку
  (если есть зеркало), либо положи `.ttf` в `assets/fonts/` и впиши `@font-face`
  в `local_faces`. Иначе останется системный шрифт — сайт не сломается.
- `hero` — заголовок и главное фото; `slides` — 4 фото слайдера (одинакового размера!);
  `features`, `contacts` — блоки главной.
- Картинки-заглушки в `assets/img/` замени на фото из задания (имена те же).

### Файл `config.php` — ДАННЫЕ и ЛОГИКА
- `courses[]` — список курсов/услуг (≥10 для базы).
- `payment_types[]`, `statuses[]` — если в задании другие.
- `admin` — логин/пароль администратора из задания.
- `entity_label*` — как называется заявка.

### Сброс базы после смены config
phpMyAdmin → база `exam_db` → Operations → Drop, затем обнови страницу.

---

## 3. Что закрывает каждый модуль

| Требование | Где |
|---|---|
| Главная с наполнением + слайдер (4 фото, 3 сек, вперёд/назад) | `index.php`, `theme.php`, `assets/app.js` |
| Регистрация: логин латиница+цифры ≥6 уникальный, пароль ≥8, ФИО, телефон 8(XXX)XXX-XX-XX, email | `register.php`, `helpers.php` |
| Авторизация + сообщения + ссылка на регистрацию | `login.php` |
| Кабинет: свои заявки + отзыв после завершения обучения | `cabinet.php` |
| Форма заявки: курс из списка, дата ДД.ММ.ГГГГ, оплата | `order.php` |
| Админка `Admin`/`KorokNET`: статусы + фильтр + поиск + пагинация + тосты | `admin.php`, `admin_login.php` |
| Отзывы (на главной) и форма обратной связи | `index.php`, БД `reviews`, `feedback` |
| **Подсистема безопасности (8 баллов)** | `security.php`, `.htaccess`, `docs/БЕЗОПАСНОСТЬ.md` |
| Адаптив 390×844 + анимации | `assets/style.css` |
| Мобильные макеты (отдельный .html на страницу) | `mobile/` |
| ER-диаграмма + ERwin | `docs/ER.png`, `docs/ER.svg`, `docs/ER.md`, `sql/erwin_reverse.sql` |
| Готовый дамп + 10+ строк | `database.sql`, `sql/` |

---

## 4. Подсистема безопасности — показать эксперту (это 8 баллов!)
Открой `security.php` и `docs/БЕЗОПАСНОСТЬ.md`. Кратко защищено от:
SQL-инъекций (подготовленные запросы), XSS (экранирование + CSP), CSRF (токены в формах),
брутфорса/DDoS (rate limit), кражи сессии, открытых паролей (хеширование) + HTTP-заголовки.

---

## 5. Частые проблемы
- **Ошибка подключения к MySQL** — не запущен MySQL, либо пароль в `config.php` неверный.
- **Текст файла вместо страницы / «лишний вывод»** — где-то в PHP-комментарии попался
  символ `?>` (он закрывает PHP). Убери его из комментария.
- **Крякозябры** — таблицы и файлы должны быть в UTF-8 (у нас так).
- **Слайдер/маски не работают** — проверь, что подключён `assets/app.js` (он в подвале).

---

## 6. Порядок на экзамене
1. Выпиши из задания: название, цвета/лого/фото, список курсов, поля заявки, статусы, доступ админа.
2. Внеси внешность в `theme.php`, данные — в `config.php`.
3. Проверь запуск, регистрацию, заявку, админку.
4. Покажи эксперту подсистему безопасности (`security.php`).
5. Сделай ER-диаграмму и мобильные макеты, пройди чек-лист из п.3.
