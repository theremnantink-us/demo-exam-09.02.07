-- ============================================================================
-- 01_create_database.sql — создание базы данных.
-- Вставь это первым в phpMyAdmin (вкладка SQL), если базы ещё нет.
-- ============================================================================
CREATE DATABASE IF NOT EXISTS exam_db
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;

USE exam_db;
